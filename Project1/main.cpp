#include <iostream>
#include <stdio.h>

using std::cout;
using std::endl;

// Literal 은 소스 코드 상에서 고정된 값을 가지는 것을 의미한다.
// ""로 묶인 값들은 문자형 리터럴로 이해한다.
int main() {
    // 밑은 상수라고 생각하면 된다.
    char *pstr = "goodbye";
    printf("why so serious");
    return 0;
}
